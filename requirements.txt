# Core dependencies
requests>=2.25.0

# Optional LLM providers (install as needed)
openai>=1.0.0
anthropic>=0.3.0

# Optional automation libraries (install as needed)
beautifulsoup4>=4.9.0
schedule>=1.1.0
twilio>=8.0.0

# Optional data processing
gspread>=5.0.0
watchdog>=2.1.6
croniter>=1.0.15
